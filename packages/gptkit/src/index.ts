/*
  This package is a convenience package that re-exports the core and chat, and
  any other packages that are added to the GPTKit ecosystem. This is the
  package that should be used by the end user to import the GPTKit library.
*/
export { setOpenAIKey } from '@gptkit/core'
export { createChat } from '@gptkit/chat'
